import Character from './Character';
import characters from './person';
import Team from './Team';

const team = new Team();
team.addAll(...characters);

console.log(`map.get('${this.members}') = ${Character}`);
